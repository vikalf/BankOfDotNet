@{
    ViewData["Title"] = "Secure Page";
}

<div class="text-center">
    <h1 class="display-4">Secure</h1>
</div>

<h3>User Claims</h3>

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dt>@claim.Value</dt>
    }
</dl>